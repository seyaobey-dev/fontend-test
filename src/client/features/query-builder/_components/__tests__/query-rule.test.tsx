import { describe, it } from "node:test";

describe("QueryRule Component", () => {
    it("selects an operator when the operator is clicked", () => {
    });

    it("adds a new rule when the + Rule button is clicked", () => {
    });

    it("adds a new group when the + Group button is clicked", () => {
    });
});